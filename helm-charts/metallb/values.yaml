# Address pool handed out for LoadBalancer services (was previously
# hard-coded in ansible-playbooks/kubernetes/06-deploy-metallb.yaml).
ipAddressPool:
  name: default-pool
  addresses:
    - "192.168.86.40-192.168.86.50"
